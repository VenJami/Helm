# dependencies + build output
node_modules/
web/dist/
*.tsbuildinfo

# local Claude Code settings (personal machine config)
.claude/settings.local.json

# local design references (third-party screenshots — not for the public repo)
docs/context/

# logs
npm-debug.log*

# OS cruft
Thumbs.db
.DS_Store
